/** FEEDBACK: Here you can use .replace to update the characters and use test to check if they are lowercase or uppercase! */
const swapAllCases = (str) => {
  const regex = /[a-zA-Z]/g
  return str.replace(regex, )
};

module.exports = {
  swapAllCases,
};
